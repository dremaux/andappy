<form method="post" action="{{ path('option_delete', {'id': option.id}) }}" onsubmit="return confirm('êtes vous vraiment sûr ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ option.id) }}">
    <button class="btn">Delete</button>
</form>
